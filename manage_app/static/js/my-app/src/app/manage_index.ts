import { enableProdMode } from '@angular/core';
import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';

import { ManageIndexModule } from './app/manage_index/manage_index.module';

platformBrowserDynamic().bootstrapModule(ManageIndexModule);
